# Screenshots

Add release screenshots here:

- Main dark-mode scan result.
- Inspector panel with selected folder.
- Treemap and largest files analysis.
- Safe deletion confirmation and Reclaim Meter.

Use `python scripts/render_screenshots.py` for local screenshots. On Windows, run it from a normal desktop session for release images. Forced Qt offscreen rendering may use a reduced font backend and can differ from the real app window.
